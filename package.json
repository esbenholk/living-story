{
  "name": "living-story",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "yarn install && yarn --cwd backend install && yarn --cwd frontend install",
    "dev": "concurrently --names \"BACKEND,FRONTEND,SIDECAR\" --prefix-colors \"blue,green,magenta\" \"yarn --cwd backend dev\" \"yarn --cwd frontend dev\" \"yarn dev:sidecar\"",
    "dev:sidecar": "cd sidecar && python app.py",
    "start:backend": "yarn --cwd backend dev",
    "start:frontend": "yarn --cwd frontend dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}
